<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    @*<div class="navbar">
        <div class="container">
            <div class="navbar-header">
                <a href="@Url.Action("Index", "Home")">
                    <img src="~/Content/Images/logo_hb.png" style="width: auto; height: 60px;" />
                </a>
                <a href="@Url.Action("Index", "Home")" style="padding-left: 25px;">
                    <img src="~/Content/Images/logo_siir.png" style="width: auto; height: 60px;" />
                </a>
            </div>
        </div>
    </div>*@
    <div class="navbar navbar-inverse navbar-default navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                @*@Html.ActionLink("Hem", "Index", "Home", null, new { @class = "navbar-brand" })*@
                <span class="navbar-brand">@{Html.RenderAction("CartSummary", "ShoppingCart");}</span>
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Produkter", "Index", "Store")</li>
                    <li></li>

                </ul>
            </div>
        </div>
    </div>

    <div class="container body-content">
        @RenderBody()
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>