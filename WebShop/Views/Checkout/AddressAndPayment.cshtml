@model WebShop.Models.Order

@{
    ViewBag.Title = "Address And Payment";
}

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    
    

    <fieldset>
        <h2>Användaruppgifter</h2>
        <hr />

        @Html.ValidationSummary(true)

        @*<div style="margin-bottom: 13px;">
            <h4>@Html.DisplayNameFor(x => x.Username)</h4>
            @Html.EditorFor(x => x.Username)
            @Html.ValidationMessageFor(x => x.Username)
        </div>*@
        <div style="margin-bottom: 13px;">
            <h4>@Html.DisplayNameFor(x => x.Age)</h4>
            @Html.EditorFor(x => x.Age)
            @Html.ValidationMessageFor(x => x.Age)
        </div>
        <div style="margin-bottom: 15px;">
            <h4>@Html.DisplayNameFor(x => x.Sex) </h4>
            @Html.DropDownList("Kön", new List<SelectListItem>()
               {
                   new SelectListItem() { Text = "Man", Value = "Man"},
                   new SelectListItem() { Text = "Kvinna", Value = "Kvinna"}
               }, "")

            @Html.ValidationMessageFor(x => x.Sex)
        </div>

        @*@Html.EditorForModel()*@
    </fieldset>
    
    <br />
        
    <input type="submit" value="Avsluta köp" />
}